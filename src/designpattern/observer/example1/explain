Head First 设计模式中的观察者模式示例代码1

Subject：主题接口
Observer：所有的组件都实现此观察者接口，这样主题在需要通知观察者时，就有了一个共同的接口。实现此接口的人都可以收到主题每次发送的消息。
DisplayElement：布告板接口，实现display()方法。
WeatherData：实现主题接口，具体化主题。(Subject是一个主题接口，当你想要哪方面的主题时，实现Subject接口，
                具体化自己的主题，然后看观察者需要订阅哪个主题，订阅该主题即可)。
